{
  "version": 2,
  "builds": [
    { "src": "backend/*.js", "use": "@vercel/node" },
    { "src": "backend/frontend/build/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1" },
    { "src": "/(.*)", "dest": "frontend/build/$1" }
  ]
}
